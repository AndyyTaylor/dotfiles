input type:pointer {
    scroll_factor 2.0
    # Slower cursor. libinput's accel_speed is -1..1 (0 = stock), and on the
    # adaptive profile it reshapes the accel curve rather than acting as a
    # straight multiplier, so -0.2 is "about 20% slower", not exactly.
    pointer_accel -0.2
}
